# 🌳 Preorder Traversal in Binary Tree (BT) [#144](https://leetcode.com/problems/binary-tree-preorder-traversal/description/)

## 📌 Problem Statement

Given the **root of a Binary Tree**, perform **Preorder Traversal** and return the sequence of visited nodes.

👉 **Preorder Traversal Order**:

```
Root → Left → Right
```



## 🧠 Why Preorder Traversal matters

Preorder traversal is all about **doing things early**:

* The **root is processed first**
* Useful when:

  * Creating a **copy of a tree**
  * **Serializing / deserializing** trees
  * Evaluating **expression trees**
  * Printing tree structure

If inorder is polite and patient,
preorder is like:

> “Hi, I’m here. Now let me check my kids.” 😄



## 👀 Example

Binary Tree:

```
        1
       / \
      2   3
     / \
    4   5
```

### Preorder Traversal Output

```
1 2 4 5 3
```



## 🧩 Intuition (how to really remember it)

At every node:

1. **Visit the current node**
2. Traverse the **left subtree**
3. Traverse the **right subtree**

You always announce yourself **before** exploring children.



## 🐌 Brute Force Approach (conceptual)

There’s no true brute-force here, but beginners sometimes:

* Revisit nodes unnecessarily
* Store paths redundantly
* Confuse traversal order

👉 Clean recursion or stack-based traversal avoids all that.



# ✅ Optimal Approaches

We’ll cover:

1. Recursive Preorder (clean & intuitive)
2. Iterative Preorder (stack-based, interview favorite)
3. Morris Preorder (O(1) space, Jedi level 🧘‍♂️)



## 1️⃣ Recursive Preorder Traversal 🌱

### 🧠 Idea

Recursion mirrors the tree’s structure:

* Process root
* Go left
* Go right



### 💡 Algorithm

```
If root is NULL:
    return

Visit root
Preorder(root.left)
Preorder(root.right)
```



### 💻 Code (C++)

```cpp
void preorder(Node* root, vector<int>& result) {
    if (!root) return;

    result.push_back(root->val);
    preorder(root->left, result);
    preorder(root->right, result);
}
```



### ✅ Why this works

* Each node is visited once
* Very readable
* Great for learning & quick coding



### ⏱ Complexity

* **Time:** O(N)
* **Space:** O(H) recursion stack



## 2️⃣ Iterative Preorder Traversal (Using Stack) 🧱

🔥 **Most asked version in interviews**



### 🧠 Intuition

Simulate recursion manually:

* Use a stack
* Process root first
* Push **right child before left child**

Why?
Because stack is **LIFO**, and we want left processed first.



### 💡 Algorithm

1. Push root into stack
2. While stack not empty:

   * Pop node
   * Visit it
   * Push right child
   * Push left child



### 💻 Code (C++)

```cpp
vector<int> preorderTraversal(Node* root) {
    vector<int> result;
    if (!root) return result;

    stack<Node*> st;
    st.push(root);

    while (!st.empty()) {
        Node* curr = st.top();
        st.pop();

        result.push_back(curr->val);

        if (curr->right) st.push(curr->right);
        if (curr->left) st.push(curr->left);
    }

    return result;
}
```



### ⏱ Complexity

* **Time:** O(N)
* **Space:** O(H)



### 💬 Interview Tip

For preorder:

* **Right pushed first**
* **Left processed first**

Mess this up → traversal order breaks 🚨



## 3️⃣ Morris Preorder Traversal 🌟 (O(1) Space)

> “Why use a stack when the tree itself can remember?” 😏



### 🧠 Idea

* Use temporary **threads** to predecessor
* No recursion
* No stack
* Tree restored afterward



### 💡 Key Difference from Inorder Morris

* In preorder, **visit node when creating the thread**, not when removing it.



### 💻 Code (C++)

```cpp
vector<int> preorderMorris(Node* root) {
    vector<int> result;
    Node* curr = root;

    while (curr) {
        if (!curr->left) {
            result.push_back(curr->val);
            curr = curr->right;
        } else {
            Node* prev = curr->left;
            while (prev->right && prev->right != curr)
                prev = prev->right;

            if (!prev->right) {
                result.push_back(curr->val); // visit before threading
                prev->right = curr;
                curr = curr->left;
            } else {
                prev->right = nullptr;
                curr = curr->right;
            }
        }
    }
    return result;
}
```



### ⏱ Complexity

* **Time:** O(N)
* **Space:** O(1) 😎



### ⚠️ Caution

* Easy to make mistakes
* Use only when asked about space optimization



# 🧪 Test Cases

| Tree Type     | Preorder Output    |
| - |  |
| Empty tree    | []                 |
| Single node   | [1]                |
| Left skewed   | root → bottom      |
| Right skewed  | same as tree order |
| Balanced tree | Root-first pattern |



# 🧠 Key Observations

* Root is always **first**
* Stack order matters
* Preorder + Inorder → **unique tree reconstruction**



# 🚀 Variations & Related Problems

* Serialize & Deserialize Binary Tree
* Construct tree from Preorder & Inorder
* Expression tree evaluation
* Flatten Binary Tree to Linked List
* Mirror tree



# ❓ FAQs

### Q1: Why is preorder useful for serialization?

Because root comes first, structure can be rebuilt easily when combined with inorder.



### Q2: Recursive vs Iterative preorder?

* Recursive → easier to write
* Iterative → better control, avoids stack overflow



### Q3: Why push right child before left in stack?

Stack is LIFO.
Push right first so left gets processed first.



### Q4: Can preorder detect BST?

No directly.
But preorder + constraints can be used to validate BST.



### Q5: Is Morris preorder safe?

Yes, if implemented correctly.
Tree is restored at the end.



# 🧠 Quick Revision Cheat Sheet 📝

```
Preorder = Root → Left → Right
Recursive = clean
Iterative = push right first
Morris = visit before threading
```

